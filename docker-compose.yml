version: '3.8'

services:
  worker:
    image: python:3.11-slim
    volumes:
      - ./:/app
    working_dir: /app
    command: sh -c "while true; do sleep 3600; done"
  redis:
    image: redis:7
    ports:
      - "6379:6379"
